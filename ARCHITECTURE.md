# 足球预测系统 — 模型训练方案 & 预测架构设计

## 一、现状分析

### 当前架构
```
数据源 → MySQL → Dixon-Coles 单模型 → API → 前端
```

### 核心问题
1. **单模型依赖**：仅 Dixon-Coles 泊松模型，无冗余
2. **特征单一**：仅用历史比分，未利用赔率、xG、阵容、状态等
3. **训练无验证**：无 train/val/test 切分，无法检测过拟合
4. **预测慢**：伤病检测每次查询 44 次数据库
5. **无模型版本管理**：无法回滚或 A/B 测试

---

## 二、新架构设计

### 2.1 整体架构
```
┌─────────────────────────────────────────────────────────────┐
│                      数据层 (Data Layer)                      │
│  football-data.co.uk │ Transfermarkt │ TheSportsDB │ 赔率   │
└──────────────────────┬──────────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────────┐
│                   特征工程 (Feature Store)                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 球队特征  │ │ 球员特征  │ │ 对战特征  │ │ 环境特征  │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
└──────────────────────┬──────────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────────┐
│                   模型层 (Model Ensemble)                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │Dixon-Coles│ │Poisson   │ │XGBoost   │ │Elo Rating│       │
│  │  (基线)   │ │ 回归     │ │ 分类     │ │  (辅助)   │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
│                       │                                      │
│              ┌────────▼────────┐                             │
│              │  集成学习 (Stack) │                             │
│              │  逻辑回归 / NN    │                             │
│              └────────┬────────┘                             │
└───────────────────────┼──────────────────────────────────────┘
                        │
┌───────────────────────▼──────────────────────────────────────┐
│                   预测服务 (Prediction Service)                │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                     │
│  │ 模型缓存  │ │ 特征缓存  │ │ 结果缓存  │                     │
│  └──────────┘ └──────────┘ └──────────┘                     │
│                       │                                      │
│              ┌────────▼────────┐                             │
│              │   API + 蒙特卡洛  │                             │
│              └─────────────────┘                             │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 数据流水线 (Data Pipeline)

```
每日 06:00 ──→ 拉取昨日比分 + 赔率
每日 12:00 ──→ 拉取今日赛程 + 阵容
每小时    ──→ 拉取实时比分 (比赛日)
每周      ──→ 更新 Transfermarkt 球员/伤病数据
每月      ──→ 重训练模型
```

---

## 三、特征工程 (Feature Store)

### 3.1 球队特征 (Team Features)

| 特征名 | 说明 | 来源 | 更新频率 |
|--------|------|------|----------|
| `attack_strength` | DC 模型进攻参数 α | DC 模型 | 每月 |
| `defence_strength` | DC 模型防守参数 β | DC 模型 | 每月 |
| `elo_rating` | Elo 积分 | 历史比分 | 每场 |
| `form_5` | 近 5 场加权积分 | 历史比分 | 每场 |
| `form_10` | 近 10 场加权积分 | 历史比分 | 每场 |
| `home_attack` | 主场进攻强度 | 主场比分 | 每月 |
| `home_defence` | 主场防守强度 | 主场比分 | 每月 |
| `away_attack` | 客场进攻强度 | 客场比分 | 每月 |
| `away_defence` | 客场防守强度 | 客场比分 | 每月 |
| `xg_for` | 近 10 场 xG (进攻) | FBref | 每周 |
| `xg_against` | 近 10 场 xG (防守) | FBref | 每周 |
| `shots_on_target_pct` | 射正率 | 历史比分 | 每月 |
| `possession_avg` | 平均控球率 | FBref | 每周 |
| `corners_avg` | 平均角球数 | 历史比分 | 每月 |

### 3.2 对战特征 (Matchup Features)

| 特征名 | 说明 | 来源 |
|--------|------|------|
| `h2h_home_wins` | 近 5 年主场胜率 | 历史比分 |
| `h2h_draws` | 近 5 年平局率 | 历史比分 |
| `h2h_avg_goals` | 近 5 年场均进球 | 历史比分 |
| `market_odds_home` | 市场赔率 (主胜) | 赔率数据 |
| `market_odds_draw` | 市场赔率 (平局) | 赔率数据 |
| `market_odds_away` | 市场赔率 (客胜) | 赔率数据 |
| `implied_prob_home` | 隐含概率 (主胜) | 赔率换算 |
| `odds_movement` | 赔率变动幅度 | 赔率时序 |

### 3.3 球员特征 (Player Features)

| 特征名 | 说明 | 来源 |
|--------|------|------|
| `squad_avg_overall` | 阵容平均评分 | Transfermarkt |
| `squad_max_overall` | 最高球员评分 | Transfermarkt |
| `injury_count` | 伤病人数 | 出场记录 |
| `injury_impact` | 伤病影响系数 | 出场记录 |
| `key_player_available` | 核心球员是否在阵 | 出场记录 |
| `avg_age` | 阵容平均年龄 | Transfermarkt |
| `market_value_total` | 阵容总市值 | Transfermarkt |

### 3.4 环境特征 (Context Features)

| 特征名 | 说明 |
|--------|------|
| `is_derby` | 是否德比 |
| `match_importance` | 比赛重要性 (1-5) |
| `days_since_last` | 距上场比赛天数 |
| `fixture_congestion` | 近 14 天比赛数 |
| `is_home` | 是否主场 |

---

## 四、模型方案

### 4.1 模型 1: Dixon-Coles (基线模型)

**用途**：提供泊松分布基线，计算期望进球

```
λ = exp(α_home + β_away + γ)
μ = exp(α_away + β_home)

P(X=i, Y=j) = τ(i,j,λ,μ) × Poisson(i,λ) × Poisson(j,μ)
```

**训练参数**：
- 训练窗口：最近 2 个赛季
- 最少样本：100 场比赛
- 优化器：L-BFGS-B
- 约束：ρ ∈ [-0.5, 0.5], γ ∈ [-1.0, 1.0]

### 4.2 模型 2: 特征增强泊松回归

**用途**：利用更多特征提升预测精度

```
log(λ) = β₀ + β₁·elo_diff + β₂·form_diff + β₃·xg_diff 
         + β₄·injury_impact + β₅·market_prob + β₆·is_derby
         + β₇·fixture_congestion + ...

log(μ) = γ₀ + γ₁·elo_diff + γ₂·form_diff + ...
```

**训练**：
- 特征标准化 (Z-score)
- 正则化 (L2, α=0.1)
- 交叉验证 (5-fold)

### 4.3 模型 3: XGBoost 分类器

**用途**：直接预测 H/D/A 结果

```
输入特征: [elo_diff, form_5_diff, xg_diff, injury_impact, 
           market_implied_prob, h2h_stats, is_derby, ...]
输出: P(H), P(D), P(A)
```

**训练参数**：
- 树深度：6
- 学习率：0.05
- 子采样：0.8
- 早停轮数：50

### 4.4 模型 4: Elo 评分系统

**用途**：提供长期实力评估，作为其他模型的输入特征

```
E_A = 1 / (1 + 10^((R_B - R_A) / 400))
R_A_new = R_A + K × (S_A - E_A)

K = 20 × (1 + goal_diff_bonus)
```

**初始化**：
- 新赛季：R_new = R_old × 0.7 + 联赛平均 × 0.3
- 新升级球队：1500

### 4.5 集成策略 (Stacking)

```
┌─────────────────────────────────────────┐
│  Layer 1: 基模型                         │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐   │
│  │DC 模型   │ │泊松回归  │ │XGBoost  │   │
│  │P(H),P(D)│ │P(H),P(D)│ │P(H),P(D)│   │
│  │P(A),xG  │ │P(A),xG  │ │P(A)     │   │
│  └────┬────┘ └────┬────┘ └────┬────┘   │
│       └───────────┼───────────┘         │
│                   ▼                     │
│  Layer 2: 元学习器                       │
│  ┌─────────────────────────────────┐    │
│  │  逻辑回归 / 小型神经网络          │    │
│  │  输入: 各模型预测概率             │    │
│  │  输出: 最终 P(H), P(D), P(A)     │    │
│  └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
```

---

## 五、训练流水线

### 5.1 训练流程

```
1. 数据准备
   ├── 拉取最近 N 赛季比赛数据
   ├── 计算所有特征
   ├── 划分 train/val/test (70/15/15)
   └── 特征标准化

2. 模型训练
   ├── DC 模型: 全量训练
   ├── 泊松回归: 5-fold CV
   ├── XGBoost: 早停 + 5-fold CV
   └── Elo: 在线更新

3. 集成训练
   ├── 用 val 集生成基模型预测
   ├── 训练元学习器
   └── 评估集成效果

4. 模型评估
   ├── 准确率 (Accuracy)
   ├── Brier Score
   ├── Log Loss
   ├── 校准曲线 (Calibration)
   └── 按赔率分桶 ROI

5. 模型保存
   ├── 版本号: v{date}_{league}
   ├── 保存: 模型参数 + 特征列表 + 元学习器
   └── 元数据: 训练日期、样本数、评估指标
```

### 5.2 训练调度

| 任务 | 频率 | 说明 |
|------|------|------|
| Elo 更新 | 每场比赛后 | 在线更新 |
| 特征刷新 | 每日 | 更新状态/伤病 |
| DC 重训练 | 每月 | 新数据积累 |
| 泊松回归重训练 | 每月 | 新数据积累 |
| XGBoost 重训练 | 每季度 | 计算密集 |
| 元学习器重训练 | 每季度 | 依赖基模型 |

---

## 六、预测服务架构

### 6.1 预测流程

```
请求到达
    │
    ▼
┌───────────────┐
│  结果缓存检查   │ ──→ 命中 ──→ 返回
│  TTL: 5 分钟   │
└───────┬───────┘
        │ 未命中
        ▼
┌───────────────┐
│  特征计算      │
│  (带缓存)      │
└───────┬───────┘
        │
        ▼
┌───────────────┐
│  并行模型推理   │
│  ┌─────┐      │
│  │ DC  │──┐   │
│  ├─────┤  │   │
│  │ PR  │──┤   │  并行执行
│  ├─────┤  ├──→│ 集成
│  │XGB  │──┤   │
│  ├─────┤  │   │
│  │ Elo │──┘   │
│  └─────┘      │
└───────┬───────┘
        │
        ▼
┌───────────────┐
│  集成 + 校准   │
└───────┬───────┘
        │
        ▼
┌───────────────┐
│  蒙特卡洛模拟   │ (可选，用于比分分布)
└───────┬───────┘
        │
        ▼
┌───────────────┐
│  缓存 + 返回   │
└───────────────┘
```

### 6.2 缓存策略

| 缓存层 | TTL | 说明 |
|--------|-----|------|
| 模型参数 | 永久 (手动刷新) | 训练后更新 |
| 球队特征 | 1 天 | 每日刷新 |
| 伤病数据 | 1 小时 | 实时性要求高 |
| 预测结果 | 5 分钟 | 相同比赛+阵容 |
| 阵容数据 | 4 小时 | 比赛日更新 |

### 6.3 性能目标

| 指标 | 当前 | 目标 |
|------|------|------|
| 快速预测延迟 | ~100ms | <50ms |
| 完整预测延迟 | ~37s | <2s |
| 并发支持 | 无限制 | 100 QPS |
| 模型准确率 | ~50% | >55% |
| Brier Score | ~0.65 | <0.60 |

---

## 七、实施计划

### Phase 1: 特征工程 (1-2 周)
- [ ] 构建特征计算引擎
- [ ] 实现 Elo 评分系统
- [ ] 集成赔率数据作为特征
- [ ] 特征缓存机制

### Phase 2: 模型扩展 (2-3 周)
- [ ] 实现泊松回归模型
- [ ] 集成 XGBoost
- [ ] 构建 Stacking 集成
- [ ] 完善回测框架

### Phase 3: 性能优化 (1 周)
- [ ] 伤病检测缓存
- [ ] 特征预计算
- [ ] 并行模型推理
- [ ] 预测结果缓存

### Phase 4: 监控与迭代 (持续)
- [ ] 模型性能监控
- [ ] 自动重训练触发
- [ ] A/B 测试框架
- [ ] 预测准确性追踪

---

## 八、文件结构

```
backend/
├── features/
│   ├── __init__.py
│   ├── team_features.py      # 球队特征计算
│   ├── player_features.py    # 球员特征计算
│   ├── match_features.py     # 对战特征计算
│   ├── context_features.py   # 环境特征计算
│   ├── feature_store.py      # 特征缓存与管理
│   └── elo.py                # Elo 评分系统
│
├── models/
│   ├── __init__.py
│   ├── base.py               # 模型基类
│   ├── dixon_coles.py        # DC 模型
│   ├── poisson_regression.py # 泊松回归
│   ├── xgboost_model.py      # XGBoost
│   ├── elo_model.py          # Elo
│   ├── ensemble.py           # 集成学习
│   └── registry.py           # 模型注册与版本管理
│
├── training/
│   ├── __init__.py
│   ├── pipeline.py           # 训练流水线
│   ├── evaluator.py          # 模型评估
│   └── scheduler.py          # 训练调度
│
├── engine/                   # 现有引擎 (保留)
│   ├── predictor.py
│   ├── full_predictor.py
│   └── ...
│
└── api/
    └── predict.py            # 预测 API
```
